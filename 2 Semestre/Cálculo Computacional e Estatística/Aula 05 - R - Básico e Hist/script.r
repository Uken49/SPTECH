# A linguagem R foi criada para analise de dados

# Lê o que é digitado no prompt
readline(prompt = 'Digite algo')

# Exibindo uma mensagem
print('Mensagem ou váriavel')

# Convertendo para inteiro
peso <- as.integer('64')

# Convertendo para double
altura <- as.double('1.71')

# 'Atribuição' em R é <-
a <- 'String'
a = 'String' # Querendo ou não da no mesmo que o de cima

b <- 49

# Remove a váriavel da memória
rm(variavel)

# 'c' - combine: Exibe um vetor dos elementos
vetor <- c(10,2,3,4,5,6,7)

# Atribui uma lista do primeiro número ao último
x<-(1:5)

# 'cat' - Concatena os elementos em uma String
cat("numero de 1 a 5", x)
